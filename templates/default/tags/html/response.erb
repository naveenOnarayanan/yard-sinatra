<% if object.has_tag?(:response) %>
  <div class="examples">
    <p class="tag_title">Response:</p>
    <ul class="param">
      <% object.tags(:response).each do |tag| %>
      <li>
        <span class="name"><%= tag.name %></span>
        <span class="type">"("<tt><%= tag.type %></tt>")"</span>
		        "
	      
	      
	      
	        —
	        "
	    <div class="inline">
	      <p><%= tag.text %></p>
	    </div>
      </li>
      <% end %>
    </ul>
  </div>
<% end %>
